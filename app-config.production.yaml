app:
  baseUrl: https://backstage-demo-xyz.a.run.app # You'll update this after first deploy

backend:
  baseUrl: https://backstage-demo-xyz.a.run.app
  listen:
    port: 7007
  database:
    client: better-sqlite3
    connection: './data/backstage.sqlite'
  cors:
    origin: https://backstage-demo-xyz.a.run.app
